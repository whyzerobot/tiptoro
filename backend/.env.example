# ============================================================
# TipToro 密钥文件 .env
# ============================================================
# 此文件已加入 .gitignore，绝对不要提交到代码仓库！
#
# 使用方法：
#   cp .env.example .env   然后填写实际的值
#
# 程序启动时会自动加载此文件（通过 python-dotenv）。
# 环境变量的优先级永远高于 config/settings.yaml 中的值。
# ============================================================

# ── 运行环境切换（必填）────────────────────────────────────────
# local  = SQLite + 本地文件系统（开发调试专用）
# cloud  = PostgreSQL + 阿里云 OSS（生产环境）
TIPTORO_ENV=local

# ── 数据库密钥（cloud 环境必填）──────────────────────────────
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tiptoro
DB_USER=
DB_PASSWORD=

# ── 大模型 API Keys（填写你要启用的模型）────────────────────
# 留空则该 Provider 不可用，系统自动降级到 fallback
DEEPSEEK_API_KEY=
GEMINI_API_KEY=
OPENAI_API_KEY=
GROK_API_KEY=
MINIMAX_API_KEY=
MINIMAX_GROUP_ID=

# ── 对象存储密钥（cloud 环境必填）────────────────────────────
# 阿里云 OSS
OSS_ACCESS_KEY_ID=
OSS_ACCESS_KEY_SECRET=

# AWS S3（将 storage.cloud.driver 改为 aws_s3 时使用）
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=

# ── 用户认证 ────────────────────────────────────────────────────
# 必须设置一个随机字符串作为 JWT 签名密钥（本地开发可用任意字符串）
JWT_SECRET=change-me-to-a-random-secret-string

# SMTP 邮件发送（cloud 环境邮箱验证使用，local 模式下打印到控制台）
SMTP_USER=
SMTP_PASSWORD=

# ── 计费模块 ────────────────────────────────────────────────────
# 激活码 HMAC 签名密钥（必须随机，长度建议 ≥ 32 字符）
# 更换此值会导致所有已生成的激活码失效！请妥善保管。
BILLING_SECRET=change-me-to-a-random-billing-secret

# 微信支付（Phase 2，接入时填写）
# WECHAT_PAY_MCH_ID=
# WECHAT_PAY_API_V3_KEY=
# WECHAT_PAY_APP_ID=

# 支付宝（Phase 2，接入时填写）
# ALIPAY_APP_ID=
# ALIPAY_PRIVATE_KEY=
# ALIPAY_PUBLIC_KEY=

